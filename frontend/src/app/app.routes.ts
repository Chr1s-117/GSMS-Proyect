// src/app/app.routes.ts

import { Routes } from '@angular/router';

/**
 * routes
 *
 * Centralized routing configuration for the Angular application.
 *
 * Responsibilities:
 * - Defines all navigable paths and associated components.
 * - Maintains a scalable structure for adding new pages.
 * 
 * Current Status:
 * - Empty array since no routes have been defined yet.
 * - Retained to ensure consistent routing structure as the app grows.
 */
export const routes: Routes = [];
